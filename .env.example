# Local environment overrides (copy to .env and edit)

# Absolute path to your cora repository (optional; defaults to this repo root)
# CORA_ROOT=/absolute/path/to/cora

# Absolute path to your coherenceism.blog repository (optional; used by staging commands)
# BLOG_ROOT=/absolute/path/to/coherenceism.blog

# Optional: where to mirror session snapshots (automatic end-of-session or manual)
# for offsite sync; see `context/documentation/cora/backup-strategy.md:1`
# Example for iCloud Drive (ensure the folder is set to “Always Keep on This Mac”):
# CORA_SYNC_DIR=~/Library/Mobile\ Documents/com~apple~CloudDocs/cora-checkpoints
